node_modules/

# Artefact de release (uploadé sur la release GitHub, pas versionné)
system.zip

# Note : packs/ (LevelDB) est un artefact de build MAIS doit être présent pour que
# Foundry charge les compendiums. On le conserve (il n'est donc PAS ignoré ici).
# La source de vérité reste scripts/data/*.mjs et src/packs/.
